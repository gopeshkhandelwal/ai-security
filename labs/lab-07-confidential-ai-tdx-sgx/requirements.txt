# Core ML Dependencies
tensorflow
scikit-learn
numpy
joblib

# Cryptography for attestation simulation
cryptography

# System info for hardware detection
psutil
py-cpuinfo

# For memory analysis simulation
pympler

# For loading .env configuration
python-dotenv

# NOTE: Gramine (for real SGX enclaves) is a SYSTEM package, not pip:
#   sudo apt install gramine
# The run_sgx_enclave.sh script will install it automatically if needed.
